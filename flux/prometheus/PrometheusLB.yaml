apiVersion: v1
kind: Service
metadata:
  name: prometheus-lb
  namespace: prometheus
  annotations:
    fluxcd.io/ignore: true
spec:
  selector:
    app: prometheus
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
  type: LoadBalancer
